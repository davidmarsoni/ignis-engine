{
  "name": "ignis-engine",
  "version": "1.0.0",
  "type": "module",
  "description": "Firebase-powered web application with Google authentication",
  "main": "public/js/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "firebase serve",
    "deploy": "npm run build && firebase deploy",
    "dev": "npm run build && firebase serve"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "uuid": "^13.0.0"
  }
}
